name: mini_project_bnsp
description: A Flutter application for online bakery shop

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  geolocator: ^14.0.0
  geocoding: ^4.0.0
  permission_handler: ^11.0.2
  cupertino_icons: ^1.0.2
  intl: ^0.19.0
  image_picker: ^1.1.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
